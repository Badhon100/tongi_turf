import 'package:flutter/material.dart';

class AppColors {
  // --- Soft Primary (Green) ---
  static const primary = Color(0xFF2E7D32); // Softer primary green
  static const primaryLight = Color(0xFF60AD5E); // Gentle soft green
  static const primaryDark = Color(0xFF1B5E20); // Deep green
  static const accentGreen = Color(0xFF66BB6A); // Calm accent

  // --- Soft Secondary (Teal) ---
  static const secondary = Color(0xFF0097A7); // Soft teal core
  static const secondaryLight = Color(0xFF56C8D8); // Soft light teal
  static const secondaryDark = Color(0xFF006978); // Deep teal

  // --- Accent Colors ---
  static const accentYellow = Color(0xFFFFD54F); // Soft yellow
  static const accentOrange = Color(0xFFFFB74D); // Soft orange
  static const accentRed = Color(0xFFE57373); // Soft red

  // --- Background / Surface ---
  static const background = Color(0xFFF7F7F7);
  static const surface = Color(0xFFFFFFFF);

  // --- Text Colors ---
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF616161);
  static const textDisabled = Color(0xFF9E9E9E);
  static const textWhite = Color(0xFFFFFFFF);

  // --- Status Indicators ---
  static const available = Color(0xFF66BB6A);
  static const booked = Color(0xFFE57373);
  static const pending = Color(0xFFFFD54F);

  // --- Dark Theme ---
  static const darkBg = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
}
